Link API: https://classifier.dev/
SKILL chính: `bulk-classify` trong `.agents/skills`. LUÔN LUÔN dùng khi cần làm việc với `jev` API

App Language: English only
Design: Vercel -> See DESIGN.md
Tập trung ưu tiên vào X(Twitter), nền tảng khác bỏ qua, chỉ khi dc yêu cầu thì mới làm

query docs `webextensions` dùng `docs-mcp-server` để lấy best-practice bản V3
